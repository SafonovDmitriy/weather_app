import { makeStyles } from "@material-ui/core";

export default makeStyles({
  root: {
    marginTop: 60,
    transition: "all 1s",
  },
  sideBarOpen: {
    marginLeft: 200,
  },
  sideBarClose: {
    marginLeft: 80,
  },
});
