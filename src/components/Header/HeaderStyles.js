import { makeStyles } from "@material-ui/core";

export default makeStyles({
  header: {
    backgroundColor: "#7b757a52",
    height: 50,
    transition: "all 1s",
  },
  sideBarOpen: {
    marginLeft: 180,
  },
  sideBarClose: {
    marginLeft: 60,
  },
});
